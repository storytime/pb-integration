package com.github.storytime.model.aws;

/**
 * Values is saved in db by name, so names changed not allowed
 */
public enum AwsAdditionalComment {
    NBU_PREV_MOUTH_LAST_BUSINESS_DAY,
    PB_CURRENT_BUSINESS_DAY
}
